#include <stdio.h>
/**
 * main - main block
 * Return: 0 (success)
 *
 */

int main(void)
{
	int c;
	int d;
	int e = 0;

	while (e < 10)
	{
		d = 0;
		while (d < 10)
		{
			c = 0;
			while (c < 10)
			{
				if ("c != d && d != 0 < d  && d < c")
				{
					putchar(',');
					putchar(',');
				}
			}
			c++;
		}
		d++;
	}
	e++;
putchar('\n');
return (0);
}
